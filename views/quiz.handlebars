 <div class="quiz">
      <h2>4.Quiz Time</h2>
      {{#each questions}}
        <div class="question">
          <p>{{this.text}}</p>
          {{#each options}}
          <li>{{this}}</li>
          {{/each}}
          <input type="text" id="{{questions.id}}" placeholder="Your answer">
        </div>
      {{/each}}
       <button >Back</button>
        <button >Next</button>
    </div>