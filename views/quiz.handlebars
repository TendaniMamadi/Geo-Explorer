<div class="quiz">
  <h2>Quiz Time</h2>
  {{#each questions}}
  <p>{{this.question}}</p>
  <div class="question countries">
    {{#each options}}
    <div>
      <input type="radio" name="option" value="{{this}}">{{this}}
    </div>
    {{/each}}
  </div>
  {{/each}}
  <button type="submit">Submit</button>
  {{!-- <button type="submit">Next</button> --}}
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400&family=Poppins:wght@300;400&family=Young+Serif&display=swap');

  body {
    margin: 0;
    padding: 0;
    font-family: sans-serif;
    text-align: center;
  }

  header {
    text-align: center;
  }

  h1 {
    color: #ffcc00;
    background: black;
    opacity: 0.8;
    font-family: "Young Serif";
  }

  @keyframes fadeIn {
    0% {
      opacity: 0;
    }

    100% {
      opacity: 1;
    }
  }


  .animated-heading {
    animation: fadeIn 10s;
  }

  .question {
    margin-bottom: 20px;
  }


  .user-info,
  .flag-recognition,
  .moreInfo,
  .quiz,
  .phrase .problem-solving,
  .leaderboard {
    width: 45%;
    margin: 10px;
    padding: 20px;
    border: 1px solid #dee2e6;
    border-radius: 5px;
  }

  input {
    width: 100%;
    padding: 10px;
    border: 1px solid #ced4da;
    border-radius: 5px;
  }

  footer {
    position: absolute;
    text-align: center;
    bottom: 0;
    width: 100%;
    height: 60px;
    color: white;
  }


  .quiz {
    width: 90%;
    height: auto;
    background: rgba(0, 0, 0, 0.605);
    border: 1px solid white;
    box-shadow: 10px 5px 5px rgb(229, 227, 227);
    color: white;
    top: 50%;
    left: 50%;
    position: absolute;
    transform: translate(-50%, -50%);
    box-sizing: border-box;

  }

  .countries {
    display: flex;
    flex-wrap: wrap;
    width: 50%;
    justify-content: center;
  }

  .quiz button[type="submit"] {
    border: none;
    outline: none;
    width: 100px;
    height: 40px;
    background: rgb(229, 227, 227);
    color: #000000;
    font-size: 18px;
    border-radius: 20px;
  }

  .quiz button[type="submit"]:hover {
    cursor: pointer;
    background: #ffcc00;
    color: #000;
  }

  .img {
    width: 100vw;
    height: 100vh;
    background-image: url('/images/20470970.jpg');
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center center;
    background-attachment: fixed;
    overflow: hidden;
  }
</style>